# backtest/variants.py
# Окремий конфіг з варіантами моделей + майбутні опції ансамблю

# Список усіх варіантів, які очікуємо по кожному TF та side
VARIANTS = [f"V{i}" for i in range(1, 15 + 1)]  # V1..V15

# Майбутній режим голосувань:
#   "single"  – тест кожного варіанту окремо (поточний режим)
#   "kofn"    – угода відкривається, якщо >=k варіантів із N дали сигнал
#   "family"  – один варіант від кожної родини (N=кількість родин), k поріг
ENSEMBLE_MODE = "single"

ENSEMBLE_PARAMS = {
    "k": 2,            # поріг для "kofn" / "family"
    "families": None,  # якщо None -> всі родини; інакше: ["CNN","LSTM",...]
}
